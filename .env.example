# Frontend build-time variables (Vite). Copy to `.env.production` if needed.
# Base URL of the VEYRA API when it is hosted on another domain (e.g. Render).
# Leave unset to call /api on the same origin (local start.bat flow).
# VITE_API_BASE_URL=https://veyra-api.onrender.com
