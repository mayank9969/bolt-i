# Frontend build-time variables (Vite).
# Full base URL of the VEYRA API, including /api. Leave unset to call /api on
# the same origin (local start.bat flow). Production value lives in .env.production.
# VITE_API_BASE_URL=https://backend-fc24.onrender.com/api
